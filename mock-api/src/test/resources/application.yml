spring:
  datasource:
    url: jdbc:h2:mem:mock-api-test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

# 테스트에서는 지연·실패 주입을 끈다.
mock:
  chaos:
    delay-millis: 0
    failure-rate-percent: 0
